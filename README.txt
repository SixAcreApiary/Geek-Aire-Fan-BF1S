Tasmotized BF1S fan can be configured to be used over MQTT.

Internal TuyaMCU controls and stats are mapped to MQTT with the config.  Some values are dynamic/specific to my device/system.

-The generic Tasmota binary by default only exposes the top level ON/OFF ontrol to tasmota integration in homeassistant.
